{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for WakaToken",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "opener:default",
    "updater:default"
  ]
}
